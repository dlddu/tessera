# 사용자 여정 J2 — 컨테이너 워크스페이스 격리 작업

- **여정 ID**: J2
- **참조 가치 문서**: `tessera-values.md`
- **여정 인덱스**: `tessera-user-journeys.md`
- **관련 PRD / 테스트**: PRD-2 (`tessera-prd-backend.md`) / T-2 (`tessera-test-backend.md`)

## 페르소나 · 상황

- **페르소나**: macOS에서 일하는 개발자 (주 페르소나 정의는 `tessera-user-journeys.md` 참조).
- **상황**: 의존성이 까다롭거나 호스트를 더럽히고 싶지 않은 프로젝트를 시작한다. 격리 환경을 원하지만
  새 도구를 배우고 싶지는 않다.

## 달성 가치

- **V2(환경 선택의 자유)** — 주
- **V1(통합된 단일 작업 표면)** — 부 (동일한 레이아웃 조작)

## 트리거

격리가 필요한 새 프로젝트 착수.

## 단계

| 단계 | 사용자 행동 · 시스템 반응 | 근거 AC | 시각화 |
|------|---------------------------|---------|--------|
| 1 | 호스트에 깔고 싶지 않은 의존성이 걸려, 워크스페이스를 만들 때 backend를 **container**로 고르고 이미지(`node:20`)·작업 디렉토리·마운트를 지정한다. | AC2.1 | ✅ [M-J2-S1](./mockups/M-J2-S1.html) |
| 2 | 컨테이너가 뜨자 습관대로 터미널에 `hostname`·`echo $TESSERA_BACKEND`·`ls`를 쳐 본다 — 호스트가 아니라 컨테이너 안이라는 게 한눈에 확인된다. | AC2.3 | ✅ [M-J2-S2](./mockups/M-J2-S2.html) |
| 3 | 편집기 tab으로 컨테이너 안 `/workspace/src/routes/users.ts`를 열어 고치고, 같은 파일 수정을 Claude Code에도 맡긴다 — 편집도 AI 작업도 컨테이너 안에서 일어난다. | AC2.3 | ✅ [M-J2-S3](./mockups/M-J2-S3.html) |
| 4 | 작업이 커져 pane과 tab을 2×2까지 늘린다. 새로 연 tab에서 `hostname`을 쳐도 같은 컨테이너라, 어느 tab이 호스트고 어느 tab이 컨테이너인지 매번 따지지 않는다. | AC2.4 | ✅ [M-J2-S4](./mockups/M-J2-S4.html) |
| 5 | 분할 단축키가 기억나지 않아 ⌘K를 눌러보니 host 워크스페이스에서 쓰던 커맨드 목록이 그대로 뜬다 — 같은 키로 분할·포커스 이동·탭 전환을 이어서 한다. | AC2.5 | ✅ [M-J2-S5](./mockups/M-J2-S5.html) |
| 6 | 컨테이너가 무거워진 것 같아 backend 패널(⌃⌘B)을 열어 정지·재시작하고, 입력 지연 수치를 보며 호스트에서 칠 때와 체감이 다르지 않은지 확인한다. | AC2.6 | ✅ [M-J2-S6](./mockups/M-J2-S6.html) |
| 7 | 컨테이너에 없는 호스트 도구가 필요해져 **host 전용 영역**을 열고 거기서만 호스트 셸을 띄운다. 영역 경계가 화면에 보여, 지금 친 명령이 격리 밖인지 안인지 헷갈리지 않는다. | AC2.7, AC2.8 | ✅ [M-J2-S7](./mockups/M-J2-S7.html) |

## 완료 상태

도구와 조작을 그대로 유지한 채 워크스페이스 환경을 컨테이너로 선택·운용하고, 필요할 때는
컨테이너 격리를 유지하면서 선택적으로 host 전용 영역에서 호스트 도구를 실행한다 → **V2 달성**.
컨테이너가 "원격에 붙은 부속물"처럼 느껴지지 않으며, 격리에서 빠져나가는 것도 명시적 선택으로 남는다.

## 시각화 상태

이 여정의 7개 단계가 모두 mockup으로 작성·연결되었다(✅ 7/7). 갤러리는 [`mockups/index.html`](./mockups/index.html),
mockup↔여정↔가치 매핑은 [`mockups/tessera-mockup-index.md`](./mockups/tessera-mockup-index.md)를 참조한다.

> 시각화 범례: ⬜ = mockup 미작성 / ✅ = mockup 작성·연결됨. 위 표의 ✅ 항목은 `mockups/` 아래 자체 완결 HTML로 연결된다.
