# 사용자 여정 J4 — 크래시에서 작업 복원

- **여정 ID**: J4
- **참조 가치 문서**: `tessera-values.md`
- **여정 인덱스**: `tessera-user-journeys.md`
- **관련 PRD / 테스트**: PRD-4 (`tessera-prd-state-restoration.md`) / T-4 (`tessera-test-state-restoration.md`)

## 페르소나 · 상황

- **페르소나**: macOS에서 일하는 개발자 (주 페르소나 정의는 `tessera-user-journeys.md` 참조).
- **상황**: 장시간 작업 중 백엔드(컨테이너/호스트 백엔드 프로세스)나 Tessera 앱 전체가 비정상 종료된다.
  개발자는 미저장 변경과 맥락을 잃을까 걱정한다.

## 달성 가치

- **V4(작업 손실 없는 복원력)** — 주
- **V1(통합된 단일 작업 표면)** — 부 (AC1.5 레이아웃 골격 기반)

## 트리거

백엔드 또는 앱의 비정상 종료.

## 사전 상태

편집기 미저장 버퍼, 진행 중인 Claude Code 세션, 명령이 쌓인 터미널 스크롤백, 여러 개의 브라우저 탭.

## 복원 원칙 — "사용 가능한 상태"여야 한다

복원의 목표는 **실제 사용 가능한 상태**다. 백엔드(컨테이너/호스트 백엔드 프로세스)에 의존하는
컴포넌트(터미널·편집기·Claude Code)는, **죽은 백엔드가 다시 살아난 뒤 그 위에 상태를 재적용(rehydrate)해야
비로소 복원이 완료**된다. 백엔드가 죽어 있는 동안 유지되는 마지막 화면은 맥락·미저장 변경을 잃지 않기 위한
**중간 보존 뷰**일 뿐, 그 자체로 동작하는 상태가 아니다.

> 브라우저는 backend와 무관하게 항상 호스트에서 실행되므로(PRD-3) 이 제약을 받지 않는다.
> 따라서 브라우저 탭 URL 복원(AC4.4)은 백엔드 종료(시나리오 A)가 아니라 앱 종료(시나리오 B)에서 다룬다.

## 단계

| 단계 | 사용자 행동 · 시스템 반응 | 근거 AC | 시각화 |
|------|---------------------------|---------|--------|
| 1 | **(시나리오 A)** 편집 도중 컨테이너 백엔드가 죽는다. 화면이 사라지는 대신 터미널 스크롤백과 미저장 버퍼가 dim 처리된 채 남아, 어디까지 했는지 눈으로 다시 확인할 수 있다. 다만 아직 입력은 받지 않는 **읽기 전용 중간 보존 뷰**다. | AC4.1, AC4.2, AC4.3 | ✅ [M-J4-S1](./mockups/M-J4-S1.html) |
| 2 | **(A)** 배너의 '백엔드 재기동'을 눌러(또는 자동 재기동을 기다려) 죽은 컨테이너를 다시 띄운다. | AC4.5 | ✅ [M-J4-S2](./mockups/M-J4-S2.html) |
| 3 | **(A)** 백엔드가 올라오자 편집기 버퍼와 커서가 그 위로 되돌아오고 Claude 세션이 이어지며, 터미널은 새 PTY 위에 직전 스크롤백을 히스토리로 얹은 채 프롬프트를 돌려준다 — 그제야 다시 타이핑할 수 있다. | AC4.1, AC4.2, AC4.3 | ✅ [M-J4-S3](./mockups/M-J4-S3.html) |
| 4 | **(시나리오 B)** 다른 날 앱 자체가 죽어 다시 실행한다. 앱 메모리는 사라졌지만 호스트에 남은 저장분에서 pane/tab 골격·컴포넌트 상태와 브라우저 tab의 URL·순서까지 되돌아오고, 이어서 2~3과 같은 재기동·재적용이 일어난다. | AC1.5, AC4.4, AC4.5 | ✅ [M-J4-S4](./mockups/M-J4-S4.html) |
| 5 | **(공통)** 컨테이너가 꼬여 `docker rm -f`로 아예 지우고 새로 만든다. 상태는 컨테이너가 아니라 호스트에 있어서, 새로 띄운(심지어 다른 이미지의) 백엔드 위로 그대로 복원된다. | AC4.5 | ✅ [M-J4-S5](./mockups/M-J4-S5.html) |
| 6 | **(공통)** 복원하려는 파일이 그 사이 디스크에서 바뀐 것을 앱이 감지해 알려주면, 어느 쪽을 남길지 골라 정리한다 — 조용히 덮어써서 잃는 일이 없다. | AC4.6 | ✅ [M-J4-S6](./mockups/M-J4-S6.html) |

> **의존**: 백엔드 재기동 메커니즘 자체는 PRD-2의 backend 생명주기 관리(AC2.6)에 기댄다.
> 본 여정은 그 위에서 "상태 재적용을 통한 사용 가능 복원"을 다룬다.

## 완료 상태

백엔드/앱 비정상 종료 후, 죽은 백엔드를 다시 살리고 그 위로 상태를 재적용해 4종 컴포넌트가
**실제 사용 가능한 상태**로 복원된다(데이터 손실 0 목표) → **V4 달성**.
복원 결과는 동결된 화면이 아니라 곧바로 이어서 작업할 수 있는 살아있는 상태이며,
크래시가 작업·맥락의 소멸 사유가 되지 않는다.

## 시각화 상태

이 여정의 6개 단계가 모두 mockup으로 작성·연결되었다(✅ 6/6). 갤러리는 [`mockups/index.html`](./mockups/index.html),
mockup↔여정↔가치 매핑은 [`mockups/tessera-mockup-index.md`](./mockups/tessera-mockup-index.md)를 참조한다.

> 시각화 범례: ⬜ = mockup 미작성 / ✅ = mockup 작성·연결됨. 위 표의 ✅ 항목은 `mockups/` 아래 자체 완결 HTML로 연결된다.
